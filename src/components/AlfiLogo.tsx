import { cn } from "@/lib/utils";
export function AlfiMark({className}:{className?:string}){return <svg viewBox="0 0 48 48" fill="currentColor" aria-hidden="true" className={cn("size-9 shrink-0 text-[#34735c]",className)}>{Array.from({length:12},(_,i)=>{const a=i*Math.PI/6;return <circle key={i} cx={24+18*Math.cos(a)} cy={24+18*Math.sin(a)} r={2.5} opacity={.65+i%4*.08}/>})}{Array.from({length:8},(_,i)=>{const a=i*Math.PI/4;return <circle key={'m'+i} cx={24+10.5*Math.cos(a)} cy={24+10.5*Math.sin(a)} r={2} opacity={.72}/>})}<circle cx="24" cy="21" r="1.5"/><circle cx="27" cy="25.5" r="1.4"/><circle cx="21" cy="25.5" r="1.5"/></svg>}
export function AlfiLogo({className}:{className?:string}){return <span dir="ltr" role="img" aria-label="Alfi" className={cn("inline-flex items-center gap-2.5 text-[#203d32]",className)}><AlfiMark/><span aria-hidden="true" className="text-[32px] font-medium leading-none tracking-[-0.055em]">Alfi</span></span>}
